# T04：真实 CLI stale plan（2026-09-22）

状态：in_progress。本子 PR 用独立 CLI 进程生成 plan、修改受管文件，再执行
apply，验证过期计划在写 journal 前拒绝。

## 覆盖范围

- plan JSON 从独立缓存读取，用户编辑 `crates/app/src/lib.rs` 后 plan_id 不再
  匹配当前项目。
- apply 返回 `stale_plan`，用户修改保持原样。
- apply 不留下 `.gpui/upgrade/upgrade.lock`，也不创建 transactions 目录。

## 可复现验证

    cargo test --locked --test upgrade_stale_plan
    cargo fmt --check

## 尚未覆盖

真实第二个 template revision、跨进程并发 apply、磁盘满/权限错误和完整 T-06/T-07
证据仍需后续 T04 子 PR。
