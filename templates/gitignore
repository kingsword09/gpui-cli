/target
**/*.rs.bk
.DS_Store
.idea/
.vscode/

# Live-mode dev channel credentials (`gpui run --live`)
.gpui/*
!.gpui/
!.gpui/template-manifest.json

# iOS
mobile/ios/build/
mobile/ios/*.xcodeproj/
mobile/ios/*.xcworkspace/

# Android
mobile/android/gradle/.gradle/
mobile/android/gradle/build/
mobile/android/gradle/app/build/
mobile/android/gradle/local.properties
# Rust .so files are produced by `gpui build android`, never committed.
mobile/android/gradle/app/src/main/jniLibs/
